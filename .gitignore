# Entorno y secretos
.env

# Python
__pycache__/
*.pyc
.venv/
venv/

# Logs y temp
*.log
/tmp/

# dbt (proyecto dbt_fintech)
# instalado con `dbt deps`, se regenera
dbt_fintech/dbt_packages/
# artefactos compilados/ejecutados
dbt_fintech/target/
dbt_fintech/logs/
# preferencias locales del usuario
dbt_fintech/.user.yml
# contiene credenciales (documentado en README)
dbt_fintech/profiles.yml

# CI/CD local (el workflow requiere scope 'workflow' en el token; se pushea aparte)
.github/